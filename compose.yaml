name: retumador

services:

  php:
    build:
      context: ./.docker/php
    volumes:
      - .:/var/www

  nginx:
    image: nginx:latest
    volumes:
      - ./public:/var/www
      - ./.docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
